#!/bin/bash

choices="Shutdown\nRestart\nSleep"
chosen=$(echo -e $choices | dmenu -i)

case "$chosen" in
    Restart) exec systemctl reboot ;;
    Shutdown) exec shutdown now ;; 
	Sleep) i3lock -c 445544 && exec systemctl suspend ;;
    Exit) killall dwm ;; 
esac

